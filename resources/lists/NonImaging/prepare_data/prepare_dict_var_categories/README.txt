Lists with variable categories
- For each variable category a single csv file varlist_{category}.csv is created with the list of variables in that category
- Individual lists are combined into a final dictionary saved in json format
